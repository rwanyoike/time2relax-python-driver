.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install time2relax

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv time2relax
    $ pip install time2relax
